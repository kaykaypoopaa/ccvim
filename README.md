# ccvim
to install run `wget https://raw.githubusercontent.com/kaykaypoopaa/ccvim/refs/heads/main/vim.lua`

to run ccvim anywhere add this to your startup.lua `shell.setAlias("vim", "<path/to/vim.lua>")` (replace the <path/to/vim.lua> with where you saved vim.lua)
